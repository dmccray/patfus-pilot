<%= form_for(@questionnaire_template) do |f| %>
  <% if @questionnaire_template.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@questionnaire_template.errors.count, "error") %> prohibited this questionnaire_template from being saved:</h2>

      <ul>
      <% @questionnaire_template.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  
  <div class="field">
    <%= f.label :instructions %><br>
    <%= f.text_area :instructions %>
  </div>

  <div class="field">
    <%= f.collection_select :qt_type_id, QtType.all, :id, :descr %>
  </div>

  <div class="field">
    <%= f.label :comments %><br>
    <%= f.text_area :comments %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
